# Byte-compiled / optimized files
__pycache__/
*.pyc
*.pyo

# Distribution / packaging (Likely not needed yet, but good to have)
build/
dist/
*.egg-info/

# Environments (Essential!)
venv/
.venv/
env/
.env    # For environment variables
ENV/

# TkInter
*.tcl

# Unit test / coverage reports (Good to have if you add tests later)
.coverage
.pytest_cache/
htmlcov/

# Jupyter Notebook (Only if you use Jupyter)
.ipynb_checkpoints

# IDE/Editor-Specific (Keep the ones you use)
.idea/       # IntelliJ IDEA
.vscode/      # VS Code
*.swp        # Vim swap files
*~          # Emacs backup files

# macOS Finder (If you or collaborators use macOS)
.DS_Store

# Windows thumbnail cache (If you or collaborators use Windows)
Thumbs.db
ehthumbs.db
Desktop.ini

# Python general
*.log # good to exclude log files